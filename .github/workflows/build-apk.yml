name: Build Combined APK (Driver + Partner)

# Runs entirely on GitHub's own cloud servers — nothing installed on your
# phone or any computer. Triggered manually from the "Actions" tab, or
# automatically every time you push a change to the repo.
#
# This replaces build-apk-driver.yml and build-apk-partner.yml. Instead of
# two separate apps, it produces ONE apk with a home screen where the user
# picks "Driver", "Restaurant Partner", or "Bus". Your www/ folder must contain:
#   www/index.html   <- portal chooser (Driver / Partner / Bus)
#   www/driver.html  <- old driver-app.html, renamed
#   www/partner.html <- old partner-app.html, renamed
#   www/bus.html     <- bus operator app (stops + live location broadcast)
#   www/manifest.json, www/icon-192.png, www/icon-512.png (shared, as before)
#
# NOTE on permissions: bus.html and driver.html both need background
# location (ACCESS_BACKGROUND_LOCATION, FOREGROUND_SERVICE_LOCATION) so
# they keep broadcasting the driver/bus's position every so often with the
# screen off or the app swiped away — bus.html every ~20s, driver.html
# via @capgo/background-geolocation's distance-filtered updates while a
# driver is online. Because all three portals ship in one APK, Android
# grants that permission to the WHOLE app — partner.html simply never
# calls the APIs that use it, so nothing changes there.
#
# IMPORTANT: because there is now only one app package
# (com.yourcompany.neshapp instead of two), you must register a NEW
# Android app with this exact package name in your Firebase project and
# replace google-services.json at the repo root with the one Firebase
# gives you for it. The old google-services.json (registered under
# com.yourcompany.restaurantpartner) will NOT work here.
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    # Needed so the "Publish APK to a GitHub Release" step below is allowed
    # to create a release. Without this, GitHub's default token is
    # read-only and the release step fails with a 403 error.
    permissions:
      contents: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Java (needed by Android build tools)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # android-actions/setup-android@v3 used to be here, but it calls the
      # legacy `sdkmanager ... tools` command — Google discontinued that
      # 'tools' package, so the action now fails every run with "Failed to
      # find package 'tools'". GitHub's ubuntu-latest runners already ship
      # a full Android SDK (cmdline-tools included) at $ANDROID_HOME/
      # $ANDROID_SDK_ROOT, so the step was just removed rather than fixed.

      - name: Install project dependencies
        run: |
          npm install --legacy-peer-deps
          npm install @capgo/background-geolocation --legacy-peer-deps

      - name: Generate native Android project (if not already present)
        run: |
          if [ ! -d "android" ]; then
            npx cap init "NESH" "com.yourcompany.neshapp" --web-dir www
            npx cap add android
          fi

      # This repo's build has repeatedly hit a well-documented, long-
      # standing Gradle bug (gradle/gradle#25953, #28940, #29381, and
      # many duplicates — same "Failed to create Jar file ...
      # ExecutionException" signature reported against totally different
      # jars: jackson-core, tink, h2, kotlin-daemon-embeddable, and our
      # own bcprov-jdk18on) where Gradle's own classpath-instrumentation
      # step fails to repackage a plugin/buildscript-classpath jar. It's
      # not tied to one specific dependency, JDK version, or a race
      # condition (confirmed here: retries, --max-workers=1, and a JDK
      # bump all failed identically) — it's a bug in Gradle 8.2.1 itself.
      # One confirmed real-world fix for this exact error signature was
      # upgrading the Gradle version, so bump the wrapper here. Printing
      # the AGP version alongside it so if THIS also fails with a new
      # "requires Gradle version X" error, the real fix is a one-line
      # change instead of another guess.
      - name: Bump Gradle wrapper (works around a known Gradle 8.2.1 bug)
        working-directory: android
        run: |
          sed -i 's#distributionUrl=.*#distributionUrl=https\\://services.gradle.org/distributions/gradle-8.7-all.zip#' gradle/wrapper/gradle-wrapper.properties
          cat gradle/wrapper/gradle-wrapper.properties
          echo "---- AGP version in use ----"
          grep -rE "com\.android\.(application|tools\.build:gradle)" build.gradle settings.gradle 2>/dev/null || true

      # androidx.work:work-runtime:2.11.2 (pulled in by
      # @capgo/background-geolocation) requires compileSdk 35+ and AGP
      # 8.6.0+. Capacitor's default template ships AGP 8.2.1 / compileSdk
      # 34, which fails the ":app:checkDebugAarMetadata" task with a clear
      # "requires libraries... to compile against version 35" error.
      # Bumping both here fixes it.
      - name: Bump AGP and compileSdk/targetSdk (fixes work-runtime AAR metadata error)
        working-directory: android
        run: |
          sed -i "s#com\.android\.tools\.build:gradle:[0-9.]*#com.android.tools.build:gradle:8.6.0#" build.gradle
          sed -i -E "s/compileSdkVersion( *=)? *[0-9]+/compileSdkVersion = 35/" variables.gradle
          sed -i -E "s/targetSdkVersion( *=)? *[0-9]+/targetSdkVersion = 35/" variables.gradle
          echo "---- variables.gradle after bump ----"
          cat variables.gradle
          echo "---- AGP version after bump ----"
          grep "com.android.tools.build" build.gradle

      # @capgo/background-geolocation needs this Capacitor config flag on
      # Android, otherwise location updates from the WebView still halt
      # after ~5 minutes in the background — the exact bug this plugin
      # swap was meant to fix. Patched here (idempotent) since
      # capacitor.config.json is committed to the repo and cap init only
      # creates it fresh the very first time.
      - name: Enable Android legacy bridge for background-geolocation
        run: |
          node -e "
            const fs = require('fs');
            const path = 'capacitor.config.json';
            const cfg = JSON.parse(fs.readFileSync(path, 'utf8'));
            cfg.android = cfg.android || {};
            cfg.android.useLegacyBridge = true;
            fs.writeFileSync(path, JSON.stringify(cfg, null, 2));
          "

      - name: Add permissions and native components to AndroidManifest.xml
        # android/ is regenerated from scratch every run (never committed),
        # so every permission and every custom native component has to be
        # re-applied here every single build.
        run: |
          MANIFEST="android/app/src/main/AndroidManifest.xml"

          if ! grep -q "ACCESS_FINE_LOCATION" "$MANIFEST"; then
            sed -i '/<manifest /a\    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />\n    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />' "$MANIFEST"
          fi

          # Needed by bus.html (lets it keep sending location every ~20s
          # with the screen off or the app swiped away) AND by driver.html
          # (its @capgo/background-geolocation tracking, above — a driver
          # who's online keeps reporting position the same way). partner.html
          # doesn't request these, so it's unaffected and stays
          # foreground-only as before.
          if ! grep -q "ACCESS_BACKGROUND_LOCATION" "$MANIFEST"; then
            sed -i '/<manifest /a\    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />\n    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />' "$MANIFEST"
          fi

          if ! grep -q "android.permission.VIBRATE" "$MANIFEST"; then
            sed -i '/<manifest /a\    <uses-permission android:name="android.permission.VIBRATE" />\n    <uses-permission android:name="android.permission.WAKE_LOCK" />\n    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />\n    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />\n    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />\n    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />\n    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />' "$MANIFEST"
          fi

          # Lets the app trigger the system "Install this app?" prompt
          # itself (for the in-app auto-update banner) instead of routing
          # the person through their browser's download flow first. The
          # actual FileProvider <provider> entry that makes this safe
          # (sharing a downloaded APK file without exposing raw file://
          # paths) is added further down, right before </application>.
          if ! grep -q "REQUEST_INSTALL_PACKAGES" "$MANIFEST"; then
            sed -i '/<manifest /a\    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />' "$MANIFEST"
          fi

          # Register ONE AlertFcmService as the app's sole FCM receiver.
          # Firebase only allows a single FirebaseMessagingService per app,
          # which is exactly why the driver app's RideAlertFcmService and
          # the partner app's OrderAlertFcmService had to be merged into
          # one class (AlertFcmService, added further down) instead of
          # registering two.
          if ! grep -q "AlertFcmService" "$MANIFEST"; then
            python3 - "$MANIFEST" << 'PYEOF'
          import sys
          path = sys.argv[1]
          with open(path) as f:
              content = f.read()
          injection = '''
              <service android:name=".AlertFcmService" android:exported="false">
                  <intent-filter>
                      <action android:name="com.google.firebase.MESSAGING_EVENT" />
                  </intent-filter>
              </service>
              <service android:name=".RingingService" android:exported="false" android:foregroundServiceType="mediaPlayback" />
              <activity android:name=".RingingActivity"
                  android:exported="false"
                  android:launchMode="singleInstance"
                  android:excludeFromRecents="true"
                  android:showOnLockScreen="true"
                  android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" />
              <provider
                  android:name="androidx.core.content.FileProvider"
                  android:authorities="${applicationId}.fileprovider"
                  android:exported="false"
                  android:grantUriPermissions="true">
                  <meta-data
                      android:name="android.support.FILE_PROVIDER_PATHS"
                      android:resource="@xml/file_paths" />
              </provider>
          </application>'''
          content = content.replace('</application>', injection, 1)
          with open(path, 'w') as f:
              f.write(content)
          PYEOF
          fi

          # The FileProvider entry above points at this file_paths.xml,
          # which whitelists the app's own cache folder — that's where the
          # downloaded update APK gets saved (see ApkInstallerPlugin.java)
          # before the system installer is asked to open it.
          mkdir -p android/app/src/main/res/xml
          cat > android/app/src/main/res/xml/file_paths.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <paths xmlns:android="http://schemas.android.com/apk/res/android">
              <cache-path name="update_apk" path="." />
          </paths>
          EOF

      - name: Generate app icon from resources/icon.png
        run: |
          npm install -D @capacitor/assets --legacy-peer-deps
          npx capacitor-assets generate --android

      - name: Add Firebase config to the Android project
        # google-services.json must be the one Firebase generates for the
        # com.yourcompany.neshapp package (see note at the top of this file).
        run: |
          cp google-services.json android/app/google-services.json

          if ! grep -q "com.google.gms:google-services" android/build.gradle; then
            sed -i "/dependencies {/a\\        classpath 'com.google.gms:google-services:4.4.2'" android/build.gradle
          fi

          if ! grep -q "apply plugin: 'com.google.gms.google-services'" android/app/build.gradle; then
            echo "apply plugin: 'com.google.gms.google-services'" >> android/app/build.gradle
          fi

      - name: Add Firebase Messaging and AndroidX dependencies
        run: |
          if ! grep -q "firebase-messaging" android/app/build.gradle; then
            sed -i "/dependencies {/a\\    implementation 'com.google.firebase:firebase-messaging:23.4.0'\n    implementation 'androidx.annotation:annotation:1.7.1'" android/app/build.gradle
          fi

      - name: Add custom alert sounds (order + ride)
        # Both sound files are needed since a single app now handles both
        # kinds of alerts.
        run: |
          mkdir -p android/app/src/main/res/raw
          cp order_alert.mp3 android/app/src/main/res/raw/order_alert.mp3
          cp ride_alert.mp3 android/app/src/main/res/raw/ride_alert.mp3

      - name: Add native alert-handling classes
        # android/ is regenerated fresh every build, so all Java files are
        # rewritten here every time rather than committed and edited by
        # hand. AlertFcmService/RingingService/RingingActivity are shared
        # by BOTH portals; MainActivity routes to driver.html or
        # partner.html depending on which kind of alert was tapped.
        run: |
          PKG_PATH="android/app/src/main/java/com/yourcompany/neshapp"
          mkdir -p "$PKG_PATH"

          cat > "$PKG_PATH/AppState.java" << 'EOF'
          package com.yourcompany.neshapp;

          // Tracks whether an Activity of this app is currently visible, so
          // AlertFcmService can decide whether to hand an alert to the
          // already-open web app (in-app popup/ring) or start the native
          // full-screen ringing screen instead. NOTE: this is app-wide, not
          // per-portal — if you want a ride alert to still ring natively
          // while the partner portal is open (or vice versa), this flag
          // needs to be made portal-aware.
          public class AppState {
              public static volatile boolean isForeground = false;
          }
          EOF

          cat > "$PKG_PATH/MainActivity.java" << 'EOF'
          package com.yourcompany.neshapp;

          import android.app.NotificationChannel;
          import android.app.NotificationManager;
          import android.content.ContentResolver;
          import android.content.Intent;
          import android.media.AudioAttributes;
          import android.net.Uri;
          import android.os.Build;
          import android.os.Bundle;
          import com.getcapacitor.BridgeActivity;

          public class MainActivity extends BridgeActivity {
              @Override
              public void onCreate(Bundle savedInstanceState) {
                  registerPlugin(RingControlPlugin.class);
                  registerPlugin(ApkInstallerPlugin.class);
                  super.onCreate(savedInstanceState);

                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                      NotificationManager manager = getSystemService(NotificationManager.class);
                      AudioAttributes audioAttributes = new AudioAttributes.Builder()
                          .setUsage(AudioAttributes.USAGE_ALARM)
                          .setContentType(AudioAttributes.CONTENT_TYPE_SONIFICATION)
                          .build();

                      Uri orderUri = Uri.parse(ContentResolver.SCHEME_ANDROID_RESOURCE + "://" + getPackageName() + "/raw/order_alert");
                      NotificationChannel legacyChannel = new NotificationChannel(
                          "order_alerts", "Order Alerts", NotificationManager.IMPORTANCE_HIGH
                      );
                      legacyChannel.setSound(orderUri, audioAttributes);
                      legacyChannel.enableVibration(true);
                      legacyChannel.setVibrationPattern(new long[]{0, 800, 300, 800, 300, 800, 300, 800});
                      legacyChannel.setBypassDnd(true);
                      manager.createNotificationChannel(legacyChannel);
                  }

                  handleIncomingIntent(getIntent());
              }

              @Override
              public void onNewIntent(Intent intent) {
                  super.onNewIntent(intent);
                  setIntent(intent);
                  handleIncomingIntent(intent);
              }

              @Override
              public void onResume() {
                  super.onResume();
                  AppState.isForeground = true;
                  // NOTE: opening the app by itself does NOT stop
                  // RingingService any more — only an explicit accept/
                  // reject/skip action (via the RingControl plugin, called
                  // from driver.html/partner.html's own action handlers)
                  // or RingingService's own 30-second timeout clears the
                  // ringing notification. See RingControlPlugin.java.
              }

              @Override
              public void onPause() {
                  super.onPause();
                  AppState.isForeground = false;
              }

              // If this launch came from the native ringing screen (user
              // tapped Accept/Reject/Open while the app was closed), first
              // navigate the WebView to the right portal page, then forward
              // the action into that page's own JS once it has finished
              // loading. window.handleNativeRideAction (used for both
              // "ride" and "delivery" kinds — driver.html's own
              // handlePopupItemAction already branches on the kind
              // argument) / window.handleNativeOrderAction (used for
              // "order") self-retry until the page's own script is ready,
              // so exact WebView load timing doesn't matter.
              //
              // "kind" is one of "ride" / "delivery" / "order" and comes
              // straight from RingingActivity, which itself derived it from
              // the FCM push's "type" field (new_ride / new_delivery /
              // new_order). The three kinds need different id formats:
              //   ride     -> plain rideId
              //   delivery -> composite "restId::orderId" (that's the exact
              //               format driver.html's handlePopupDeliveryAction
              //               expects — see RingingActivity for where this
              //               composite id gets built)
              //   order    -> plain orderId
              private void handleIncomingIntent(Intent intent) {
                  if (intent == null) return;
                  String kind = intent.getStringExtra("kind");
                  String action = intent.getStringExtra("action");
                  String id = intent.getStringExtra("id");
                  if (kind == null || action == null) return;

                  boolean isDriverSide = "ride".equals(kind) || "delivery".equals(kind);
                  String page = isDriverSide ? "driver.html" : "partner.html";
                  String jsFunction = isDriverSide ? "handleNativeRideAction" : "handleNativeOrderAction";

                  getBridge().getWebView().post(() -> {
                      // A bare filename like "partner.html" gets treated by
                      // WebView.loadUrl() as its own domain (loads
                      // http://partner.html/ and fails with
                      // ERR_CLEARTEXT_NOT_PERMITTED) instead of a path
                      // inside the app. Capacitor serves the app's www/
                      // folder from https://localhost/ by default, so the
                      // full origin has to be included explicitly.
                      getBridge().getWebView().loadUrl("https://localhost/" + page);
                      String js = "(function retry(){"
                          + "if (window." + jsFunction + ") {"
                          + "window." + jsFunction + "("
                          + jsString(action) + "," + jsString(id != null ? id : "") + "," + jsString(kind)
                          + ");"
                          + "} else { setTimeout(retry, 300); } })();";
                      getBridge().getWebView().postDelayed(() ->
                          getBridge().getWebView().evaluateJavascript(js, null), 400);
                  });
              }

              private String jsString(String s) {
                  return "'" + s.replace("\\\\", "\\\\\\\\").replace("'", "\\\\'") + "'";
              }
          }
          EOF

          cat > "$PKG_PATH/RingControlPlugin.java" << 'EOF'
          package com.yourcompany.neshapp;

          import android.content.Intent;
          import com.getcapacitor.Plugin;
          import com.getcapacitor.PluginCall;
          import com.getcapacitor.PluginMethod;
          import com.getcapacitor.annotation.CapacitorPlugin;

          // Lets driver.html/partner.html tell the native side "the person
          // just accepted/rejected/skipped this alert from inside the
          // app" — the ONLY in-app trigger that should stop RingingService
          // early. Opening the app by itself does not call this; the
          // service otherwise only stops via its own 30-second timeout
          // (see RingingService) so it matches the Cloudflare Worker's
          // escalation window, after which the ride/order may already be
          // offered to another driver.
          //
          // JS usage: window.Capacitor.Plugins.RingControl.stopRinging()
          @CapacitorPlugin(name = "RingControl")
          public class RingControlPlugin extends Plugin {
              @PluginMethod
              public void stopRinging(PluginCall call) {
                  getContext().stopService(new Intent(getContext(), RingingService.class));
                  call.resolve();
              }
          }
          EOF

          cat > "$PKG_PATH/ApkInstallerPlugin.java" << 'EOF'
          package com.yourcompany.neshapp;

          import android.content.Intent;
          import android.net.Uri;
          import androidx.core.content.FileProvider;
          import com.getcapacitor.JSObject;
          import com.getcapacitor.Plugin;
          import com.getcapacitor.PluginCall;
          import com.getcapacitor.PluginMethod;
          import com.getcapacitor.annotation.CapacitorPlugin;
          import java.io.File;
          import java.io.FileOutputStream;
          import java.io.InputStream;
          import java.net.HttpURLConnection;
          import java.net.URL;
          import java.util.concurrent.ExecutorService;
          import java.util.concurrent.Executors;

          // Lets the in-app "UPDATE" banner (driver.html/partner.html)
          // install a downloaded APK with a single system confirmation
          // tap, instead of routing the person through the browser's
          // download flow first.
          //
          // downloadAndInstall() does the network download itself, in
          // plain Java, straight to a file — the APK bytes never cross
          // the JS-to-native bridge (which chokes on multi-megabyte
          // payloads in some Capacitor versions if the JS side tries to
          // fetch() + base64-encode + Filesystem.writeFile() a ~10 MB
          // file). JS only has to pass the download URL:
          //
          //   window.Capacitor.Plugins.ApkInstaller.downloadAndInstall({ url: apkUrl })
          //
          // install() (taking a raw file path) is kept for completeness /
          // manual use, but the update banner no longer calls it directly.
          //
          // Requires android.permission.REQUEST_INSTALL_PACKAGES (added to
          // AndroidManifest.xml) and the FileProvider <provider> entry
          // (also in AndroidManifest.xml, backed by res/xml/file_paths.xml)
          // — a bare file:// Uri would be blocked by Android for security
          // reasons on the versions this app targets.
          @CapacitorPlugin(name = "ApkInstaller")
          public class ApkInstallerPlugin extends Plugin {
              private final ExecutorService executor = Executors.newSingleThreadExecutor();

              @PluginMethod
              public void downloadAndInstall(PluginCall call) {
                  String url = call.getString("url");
                  if (url == null) {
                      call.reject("Missing 'url'");
                      return;
                  }
                  executor.execute(() -> {
                      File apkFile = new File(getContext().getCacheDir(), "update.apk");
                      HttpURLConnection conn = null;
                      try {
                          conn = (HttpURLConnection) new URL(url).openConnection();
                          conn.setInstanceFollowRedirects(true);
                          conn.connect();
                          if (conn.getResponseCode() != HttpURLConnection.HTTP_OK) {
                              call.reject("Download failed: HTTP " + conn.getResponseCode());
                              return;
                          }
                          try (InputStream in = conn.getInputStream();
                               FileOutputStream out = new FileOutputStream(apkFile)) {
                              byte[] buffer = new byte[8192];
                              int read;
                              while ((read = in.read(buffer)) != -1) {
                                  out.write(buffer, 0, read);
                              }
                          }
                          installFile(apkFile, call);
                      } catch (Exception e) {
                          call.reject("Download failed: " + e.getMessage());
                      } finally {
                          if (conn != null) conn.disconnect();
                      }
                  });
              }

              @PluginMethod
              public void install(PluginCall call) {
                  String path = call.getString("path");
                  if (path == null) {
                      call.reject("Missing 'path'");
                      return;
                  }
                  installFile(new File(path), call);
              }

              private void installFile(File apkFile, PluginCall call) {
                  try {
                      if (!apkFile.exists()) {
                          call.reject("File not found: " + apkFile.getAbsolutePath());
                          return;
                      }
                      Uri apkUri = FileProvider.getUriForFile(
                          getContext(),
                          getContext().getPackageName() + ".fileprovider",
                          apkFile
                      );
                      Intent intent = new Intent(Intent.ACTION_VIEW);
                      intent.setDataAndType(apkUri, "application/vnd.android.package-archive");
                      intent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION);
                      intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                      getContext().startActivity(intent);
                      JSObject result = new JSObject();
                      result.put("started", true);
                      call.resolve(result);
                  } catch (Exception e) {
                      call.reject("Install failed: " + e.getMessage());
                  }
              }
          }
          EOF

          cat > "$PKG_PATH/AlertFcmService.java" << 'EOF'
          package com.yourcompany.neshapp;

          import android.content.Intent;
          import androidx.annotation.NonNull;
          import androidx.core.content.ContextCompat;
          import com.google.firebase.messaging.FirebaseMessagingService;
          import com.google.firebase.messaging.RemoteMessage;
          import java.util.Map;

          // The app's sole FCM receiver, handling alerts for BOTH portals.
          // "new_ride" / "new_delivery" -> driver portal, "new_order" ->
          // partner portal. Firebase only allows one
          // FirebaseMessagingService per app, which is why this replaces
          // the driver app's RideAlertFcmService and the partner app's
          // OrderAlertFcmService.
          //
          // Each type carries its id in a different field, matching
          // exactly what the Cloudflare Worker sends and what each web
          // app's JS expects back:
          //   new_ride     -> data.rideId                         (plain)
          //   new_delivery -> data.restId + "::" + data.orderId    (composite —
          //                    driver.html's handlePopupDeliveryAction splits
          //                    on "::" to get restId/orderId back out)
          //   new_order    -> data.orderId                        (plain)
          public class AlertFcmService extends FirebaseMessagingService {
              @Override
              public void onMessageReceived(@NonNull RemoteMessage remoteMessage) {
                  super.onMessageReceived(remoteMessage);
                  Map<String, String> data = remoteMessage.getData();
                  String type = data.get("type");

                  String kind;
                  String id;
                  if ("new_ride".equals(type)) {
                      kind = "ride";
                      id = data.get("rideId");
                  } else if ("new_delivery".equals(type)) {
                      kind = "delivery";
                      id = data.get("restId") + "::" + data.get("orderId");
                  } else if ("new_order".equals(type)) {
                      kind = "order";
                      id = data.get("orderId");
                  } else {
                      return;
                  }

                  // Previously skipped entirely while the app was in the
                  // foreground (AppState.isForeground), relying only on the
                  // in-app popup/vibration from the web app's own Firebase
                  // listener. Now the native ring/notification/full-screen
                  // alert fires every time, same as when the app is closed
                  // — the in-app popup still shows too (via
                  // refreshRidePopupFromCache), so while the app is open
                  // the driver gets both together.

                  Intent serviceIntent = new Intent(this, RingingService.class);
                  serviceIntent.putExtra("kind", kind);
                  serviceIntent.putExtra("id", id);
                  serviceIntent.putExtra("title", data.get("title"));
                  serviceIntent.putExtra("body", data.get("body"));
                  ContextCompat.startForegroundService(this, serviceIntent);
              }
          }
          EOF

          cat > "$PKG_PATH/RingingService.java" << 'EOF'
          package com.yourcompany.neshapp;

          import android.app.Notification;
          import android.app.NotificationChannel;
          import android.app.NotificationManager;
          import android.app.PendingIntent;
          import android.app.Service;
          import android.content.Intent;
          import android.media.AudioAttributes;
          import android.media.MediaPlayer;
          import android.net.Uri;
          import android.os.Build;
          import android.os.Handler;
          import android.os.IBinder;
          import android.os.Looper;
          import android.os.VibrationEffect;
          import android.os.Vibrator;
          import androidx.core.app.NotificationCompat;

          // Foreground service that loops the alert sound + vibration for
          // as long as a ride or order sits un-answered, even with the app
          // fully closed. Shared by both portals; which sound/channel/label
          // it uses is picked at runtime from the "kind" extra.
          public class RingingService extends Service {
              private static final int NOTIF_ID = 4321;
              // Matches the Cloudflare Worker's NEAREST_TIMEOUT_MS exactly
              // (25s) — past this point the ride/delivery may already have
              // been offered to another driver, so this device's alert
              // should stop on its own even if nobody accepts, rejects, or
              // skips it here. Driver-side ("ride"/"delivery") only — the
              // Worker never reassigns a restaurant's own "order" alert to
              // another restaurant, so there's no matching window to
              // auto-stop against for that kind; it keeps ringing until
              // explicitly accepted/rejected in-app.
              private static final long AUTO_STOP_MS = 25_000;

              private MediaPlayer mediaPlayer;
              private Vibrator vibrator;
              private final Handler timeoutHandler = new Handler(Looper.getMainLooper());
              private Runnable timeoutRunnable;

              @Override
              public int onStartCommand(Intent intent, int flags, int startId) {
                  String kind = intent.getStringExtra("kind");
                  String id = intent.getStringExtra("id");
                  String title = intent.getStringExtra("title");
                  String body = intent.getStringExtra("body");

                  // If a second ride/order/delivery arrives while the first
                  // one is still ringing (unanswered), this service gets
                  // started again — without stopping the previous sound
                  // first, the old and new MediaPlayer would both loop at
                  // once. Always clear out any currently-playing sound and
                  // vibration before starting the new one, so only the
                  // latest alert is ever audible.
                  stopRingLoop();
                  if (timeoutRunnable != null) timeoutHandler.removeCallbacks(timeoutRunnable);

                  startForeground(NOTIF_ID, buildNotification(kind, id, title, body));
                  startRingLoop(kind);

                  boolean isDriverSide = "ride".equals(kind) || "delivery".equals(kind);
                  if (isDriverSide) {
                      timeoutRunnable = this::stopSelf;
                      timeoutHandler.postDelayed(timeoutRunnable, AUTO_STOP_MS);
                  } else {
                      timeoutRunnable = null;
                  }
                  return START_NOT_STICKY;
              }

              private Notification buildNotification(String kind, String id, String title, String body) {
                  String channelId;
                  String channelName;
                  String defaultTitle;
                  if ("ride".equals(kind)) {
                      channelId = "ride_alerts"; channelName = "Ride Alerts"; defaultTitle = "New Ride Request";
                  } else if ("delivery".equals(kind)) {
                      channelId = "delivery_alerts"; channelName = "Delivery Alerts"; defaultTitle = "New Delivery";
                  } else {
                      channelId = "order_ring_alerts"; channelName = "Order Ring Alerts"; defaultTitle = "New Order";
                  }

                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                      NotificationManager manager = getSystemService(NotificationManager.class);
                      if (manager.getNotificationChannel(channelId) == null) {
                          NotificationChannel channel = new NotificationChannel(
                              channelId, channelName, NotificationManager.IMPORTANCE_HIGH
                          );
                          channel.setSound(null, null);
                          channel.enableVibration(false);
                          manager.createNotificationChannel(channel);
                      }
                  }

                  Intent fullScreenIntent = new Intent(this, RingingActivity.class);
                  fullScreenIntent.putExtra("kind", kind);
                  fullScreenIntent.putExtra("id", id);
                  fullScreenIntent.putExtra("title", title);
                  fullScreenIntent.putExtra("body", body);
                  fullScreenIntent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TOP);
                  PendingIntent fullScreenPendingIntent = PendingIntent.getActivity(
                      this, 0, fullScreenIntent,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE
                  );

                  return new NotificationCompat.Builder(this, channelId)
                      .setSmallIcon(android.R.drawable.ic_menu_call)
                      .setContentTitle(title != null ? title : defaultTitle)
                      .setContentText(body != null ? body : "Tap to view")
                      .setPriority(NotificationCompat.PRIORITY_MAX)
                      .setCategory(NotificationCompat.CATEGORY_CALL)
                      .setFullScreenIntent(fullScreenPendingIntent, true)
                      .setContentIntent(fullScreenPendingIntent)
                      .setOngoing(true)
                      .setAutoCancel(false)
                      .build();
              }

              private void startRingLoop(String kind) {
                  try {
                      // Delivery reuses the order_alert sound file (same
                      // asset the old system used for delivery jobs) —
                      // ride gets its own ride_alert sound, and so does a
                      // restaurant order.
                      String soundName = "ride".equals(kind) ? "ride_alert" : "order_alert";
                      Uri soundUri = Uri.parse(android.content.ContentResolver.SCHEME_ANDROID_RESOURCE + "://" + getPackageName() + "/raw/" + soundName);
                      mediaPlayer = new MediaPlayer();
                      mediaPlayer.setDataSource(this, soundUri);
                      mediaPlayer.setAudioAttributes(new AudioAttributes.Builder()
                          .setUsage(AudioAttributes.USAGE_ALARM)
                          .setContentType(AudioAttributes.CONTENT_TYPE_SONIFICATION)
                          .build());
                      mediaPlayer.setLooping(true);
                      mediaPlayer.prepare();
                      mediaPlayer.start();
                  } catch (Exception ignored) {
                      // If the sound fails to load for any reason, vibration
                      // below still runs — the user still gets alerted.
                  }

                  vibrator = (Vibrator) getSystemService(VIBRATOR_SERVICE);
                  if (vibrator != null) {
                      long[] pattern = {0, 800, 300, 800, 300, 800, 300, 800};
                      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                          vibrator.vibrate(VibrationEffect.createWaveform(pattern, 0));
                      } else {
                          vibrator.vibrate(pattern, 0);
                      }
                  }
              }

              @Override
              public void onDestroy() {
                  super.onDestroy();
                  stopRingLoop();
                  if (timeoutRunnable != null) {
                      timeoutHandler.removeCallbacks(timeoutRunnable);
                      timeoutRunnable = null;
                  }
              }

              private void stopRingLoop() {
                  if (mediaPlayer != null) {
                      try { mediaPlayer.stop(); mediaPlayer.release(); } catch (Exception ignored) {}
                      mediaPlayer = null;
                  }
                  if (vibrator != null) {
                      vibrator.cancel();
                      vibrator = null;
                  }
              }

              @Override
              public IBinder onBind(Intent intent) { return null; }
          }
          EOF

          cat > "$PKG_PATH/RingingActivity.java" << 'EOF'
          package com.yourcompany.neshapp;

          import android.app.Activity;
          import android.app.KeyguardManager;
          import android.content.Intent;
          import android.graphics.Color;
          import android.os.Build;
          import android.os.Bundle;
          import android.view.Gravity;
          import android.view.WindowManager;
          import android.widget.Button;
          import android.widget.LinearLayout;
          import android.widget.TextView;

          // Full-screen "incoming alert" screen shown on top of the lock
          // screen while the app was fully closed. Shared by both portals:
          // shows Accept/Reject for rides (driver portal), Accept/Open App
          // for orders (partner portal — rejection needs a typed reason,
          // which only the web app's own modal can collect).
          public class RingingActivity extends Activity {
              private String alertId;
              private String kind; // "ride" / "delivery" / "order"

              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);

                  if (Build.VERSION.SDK_INT >= 27) {
                      setShowWhenLocked(true);
                      setTurnScreenOn(true);
                      KeyguardManager km = (KeyguardManager) getSystemService(KEYGUARD_SERVICE);
                      if (km != null) km.requestDismissKeyguard(this, null);
                  } else {
                      getWindow().addFlags(
                          WindowManager.LayoutParams.FLAG_SHOW_WHEN_LOCKED
                              | WindowManager.LayoutParams.FLAG_TURN_SCREEN_ON
                              | WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON
                              | WindowManager.LayoutParams.FLAG_DISMISS_KEYGUARD
                      );
                  }

                  kind = getIntent().getStringExtra("kind");
                  alertId = getIntent().getStringExtra("id");
                  String title = getIntent().getStringExtra("title");
                  String body = getIntent().getStringExtra("body");
                  String defaultTitle = "delivery".equals(kind) ? "New Delivery"
                      : "order".equals(kind) ? "New Order" : "New Ride Request";

                  setContentView(buildLayout(
                      title != null ? title : defaultTitle,
                      body != null ? body : ""
                  ));
              }

              private LinearLayout buildLayout(String title, String body) {
                  LinearLayout root = new LinearLayout(this);
                  root.setOrientation(LinearLayout.VERTICAL);
                  root.setGravity(Gravity.CENTER);
                  root.setPadding(60, 60, 60, 60);
                  root.setBackgroundColor(Color.parseColor("#1a1a2e"));

                  TextView titleView = new TextView(this);
                  titleView.setText(title);
                  titleView.setTextColor(Color.WHITE);
                  titleView.setTextSize(24);
                  titleView.setGravity(Gravity.CENTER);
                  root.addView(titleView);

                  TextView bodyView = new TextView(this);
                  bodyView.setText(body);
                  bodyView.setTextColor(Color.LTGRAY);
                  bodyView.setTextSize(16);
                  bodyView.setGravity(Gravity.CENTER);
                  bodyView.setPadding(0, 24, 0, 80);
                  root.addView(bodyView);

                  // Single action: open the app. No native Accept, no
                  // native Reject — every decision (accept, reject/skip)
                  // happens inside the app itself via the normal in-app
                  // popup once it's open.
                  Button openBtn = new Button(this);
                  openBtn.setText("OPEN APP");
                  openBtn.setBackgroundColor(Color.parseColor("#22c55e"));
                  openBtn.setTextColor(Color.WHITE);
                  openBtn.setOnClickListener(v -> finishWith("open"));
                  root.addView(openBtn);

                  return root;
              }

              private void finishWith(String action) {
                  // Deliberately NOT stopping RingingService here anymore —
                  // tapping OPEN APP should only open the app, not close the
                  // alert. The sound/vibration/notification keep going until
                  // the person actually accepts or cancels inside
                  // driver.html/partner.html (which calls
                  // RingControl.stopRinging()), or — driver side only — the
                  // service's own 25s AUTO_STOP_MS timeout fires.

                  Intent launch = new Intent(this, MainActivity.class);
                  launch.putExtra("kind", kind);
                  launch.putExtra("action", action);
                  launch.putExtra("id", alertId);
                  launch.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TOP);
                  startActivity(launch);
                  finish();
              }

              @Override
              public void onBackPressed() {
                  // Ignore back button — user must tap one of the buttons,
                  // same as an incoming call screen.
              }
          }
          EOF

      # ---- WHY THIS STEP EXISTS ----
      # Without it, every build used Gradle's DEFAULT debug keystore, which
      # is auto-generated on first use. GitHub Actions gives you a brand new
      # empty machine every run, so a NEW random keystore was generated on
      # every single build. Android refuses to install an APK signed with a
      # different key over an already-installed app with the same package
      # name — so every update forced an uninstall first.
      #
      # Fix: sign every build with the SAME keystore file, stored once as a
      # GitHub secret. To set this up (one-time):
      #   1. On any computer with a JDK installed, run:
      #        keytool -genkeypair -v -keystore nesh-release.keystore \
      #          -alias nesh -keyalg RSA -keysize 2048 -validity 10000
      #      (pick your own passwords; write them down — you cannot recover them)
      #   2. Base64-encode it:
      #        base64 -w0 nesh-release.keystore > keystore.b64   (Linux)
      #        base64 -i nesh-release.keystore -o keystore.b64   (macOS)
      #   3. In your GitHub repo: Settings → Secrets and variables → Actions
      #      → New repository secret. Add these 4 secrets:
      #        ANDROID_KEYSTORE_BASE64   <- contents of keystore.b64
      #        ANDROID_KEYSTORE_PASSWORD <- password you set in step 1
      #        ANDROID_KEY_ALIAS         <- "nesh" (or whatever alias you used)
      #        ANDROID_KEY_PASSWORD      <- key password you set in step 1
      #   4. IMPORTANT: back up nesh-release.keystore somewhere safe (e.g. a
      #      password manager). If you ever lose it, you can NEVER publish an
      #      update to people who already installed the app — they'd have to
      #      uninstall and reinstall fresh, one time, forever after.
      - name: Set up persistent signing keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "::warning::ANDROID_KEYSTORE_BASE64 secret not set — falling back to Gradle's throwaway debug keystore. Updates will keep forcing an uninstall until you add the 4 signing secrets (see comments in this workflow)."
          else
            echo "$KEYSTORE_BASE64" | base64 -d > "$RUNNER_TEMP/nesh-release.keystore"
          fi

      - name: Set app version + apply persistent signing config
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          GRADLE="android/app/build.gradle"
          VERSION_CODE="${{ github.run_number }}"
          VERSION_NAME="1.0.${{ github.run_number }}"

          # Every build gets a strictly increasing versionCode (the GitHub
          # Actions run number) so the app can tell "is there something
          # newer than what I have" just by comparing two integers.
          sed -i "s/versionCode [0-9]*/versionCode ${VERSION_CODE}/" "$GRADLE" || true
          sed -i "s/versionName \"[^\"]*\"/versionName \"${VERSION_NAME}\"/" "$GRADLE" || true
          if ! grep -q "versionCode" "$GRADLE"; then
            sed -i "/defaultConfig {/a\\        versionCode ${VERSION_CODE}\n        versionName \"${VERSION_NAME}\"" "$GRADLE"
          fi

          if [ -n "$KEYSTORE_BASE64" ]; then
            python3 - "$GRADLE" "$RUNNER_TEMP/nesh-release.keystore" << 'PYEOF'
          import sys, re
          gradle_path, keystore_path = sys.argv[1], sys.argv[2]
          with open(gradle_path) as f:
              content = f.read()
          signing_block = '''
              signingConfigs {
                  nesh {
                      storeFile file("%s")
                      storePassword System.getenv("NESH_KEYSTORE_PASSWORD")
                      keyAlias System.getenv("NESH_KEY_ALIAS")
                      keyPassword System.getenv("NESH_KEY_PASSWORD")
                  }
              }
          ''' % keystore_path.replace("\\", "\\\\")
          content = content.replace("android {", "android {\n" + signing_block, 1)
          # Point both debug and release build types at the same signing
          # config, so the same command (assembleDebug) always produces a
          # consistently-signed, installable-as-an-update APK.
          content = re.sub(
              r"(buildTypes\s*\{)",
              r"\1\n        debug {\n            signingConfig signingConfigs.nesh\n        }\n        release {\n            signingConfig signingConfigs.nesh\n        }",
              content,
              count=1,
          )
          with open(gradle_path, 'w') as f:
              f.write(content)
          PYEOF
          fi

      # ---- Makes the in-app "UPDATE" banner reliable ----
      # The @capacitor/app plugin isn't guaranteed to be registered in
      # every build, so instead of asking the plugin for the running
      # app's version at runtime, we bake the real build number directly
      # into the HTML here, at build time. driver.html and partner.html
      # each start with a placeholder:
      #   <script>window.NESH_BUILD_NUMBER = 0; ...</script>
      # This step replaces the 0 with this run's actual version code.
      - name: Bake build number into driver.html and partner.html
        run: |
          VERSION_CODE="${{ github.run_number }}"
          sed -i "s/window.NESH_BUILD_NUMBER = [0-9]*;/window.NESH_BUILD_NUMBER = ${VERSION_CODE};/" www/driver.html
          sed -i "s/window.NESH_BUILD_NUMBER = [0-9]*;/window.NESH_BUILD_NUMBER = ${VERSION_CODE};/" www/partner.html
          grep -o "NESH_BUILD_NUMBER = [0-9]*" www/driver.html
          grep -o "NESH_BUILD_NUMBER = [0-9]*" www/partner.html

      - name: Sync Capacitor Android project
        run: npx cap sync android

      - name: Build debug APK
        working-directory: android
        env:
          NESH_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          NESH_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          NESH_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: chmod +x gradlew && ./gradlew assembleDebug

      - name: Upload the built APK (for you to download from the Actions tab)
        uses: actions/upload-artifact@v4
        with:
          name: nesh-app-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk

      # ---- Publishes the APK somewhere phones can download it from ----
      # This is what makes the in-app "UPDATE" banner (added to
      # index.html/driver.html/partner.html) work: it points people at this
      # release's APK URL. Every push to main creates a new tagged release.
      - name: Publish APK to a GitHub Release
        id: release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: Build ${{ github.run_number }}
          files: android/app/build/outputs/apk/debug/app-debug.apk
          generate_release_notes: true

      # ---- Tells the app in people's pockets that an update exists ----
      # Writes { build, name, apkUrl } to appConfig/latestVersion in your
      # Firebase Realtime Database. The auto-update banner reads this path.
      # Needs a FIREBASE_DB_URL secret, e.g.
      #   https://your-project-id-default-rtdb.firebaseio.com
      # This only works if your Database Rules allow writes to
      # "appConfig" — if they don't, either open that one path up for
      # writes, or add a FIREBASE_DB_AUTH secret (a database secret / ID
      # token) and append `&auth=$FIREBASE_DB_AUTH` to the URL below.
      - name: Announce the new version to the app (Firebase)
        env:
          DB_URL: ${{ secrets.FIREBASE_DB_URL }}
          APK_URL: ${{ steps.release.outputs.url }}
        run: |
          if [ -z "$DB_URL" ]; then
            echo "FIREBASE_DB_URL secret not set — skipping. The in-app update banner won't know about this build until you add that secret."
            exit 0
          fi
          ASSET_URL=$(echo "$APK_URL" | sed 's#/tag/#/download/#')/app-debug.apk
          curl -s -X PUT -d "{\"build\": ${{ github.run_number }}, \"name\": \"1.0.${{ github.run_number }}\", \"apkUrl\": \"$ASSET_URL\"}" \
            "${DB_URL}/appConfig/latestVersion.json"
